# Copy to `.env` and fill in. `docker compose` reads it automatically.

# The public domain that resolves (A/AAAA) to this host. TLS is issued for it.
TZM_RELAY_DOMAIN=relay.example.com

# Contact email for Let's Encrypt (expiry notices; never shown to peers).
TZM_ACME_CONTACT=admin@example.com
